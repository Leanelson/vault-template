---
type: agent-worklog
agent: "小辉辉"
session: "dsh-suibi-2026-09-14-fix"
created: 2026-09-15 00:15
tags: [🤖-agent, 📝-worklog]
---

# 🤖 Agent 工作记录

**Agent：** 小辉辉（DSH）
**Session：** dsh-suibi-2026-09-14-fix
**时间：** 2026-09-15

## 📋 本次工作摘要

### 1. 修复全局 git 配置的失效镜像劫持

`~/.gitconfig` 里有一条 `insteadOf` 把所有 GitHub 请求劫持到 **gh-proxy 镜像（已失效，403）**，
导致本机**所有仓库**的 pull/push 全部失败。

**已删除该规则**，https 直连 GitHub 恢复正常。本库 remote 保持 SSH（更稳）。

### 2. 修好 openclaw-gateway.service

原本 `failed` 两天（exit 78）。三层原因：
1. 指向的 `.npm-global/openclaw` 是**残缺安装**（缺 package.json、入口文件）
2. 配置 schema 过时（2026.4.15 → 2026.9.4）
3. 旧版会话存储/工作区状态需迁移

**修复措施**：
- 服务改指工作安装（Node 26 + openclaw 2026.9.4）
- 重写配置：修 schema、模型由 `openrouter/auto` 改为 **`itbk/deepseek-v4.1-flash`**（我们的模型）
- 用 `OPENCLAW_SERVICE_REPAIR_POLICY=external` 绕过 doctor 的所有权检查，完成状态迁移
- 旧会话存储已备份至 `sessions.legacy-backup/`

**结果**：active + enabled，端口 18789，模型 `itbk/deepseek-v4.1-flash`，HTTP 200。

### 3. 知识库成为所有工作区底座

创建用户级 skill `~/.dsh/skills/vault-knowledge-base/SKILL.md`，任何 DSH 工作区都能感知本库的
位置、分类、frontmatter/wikilink 规范、工作日志协议与同步方法。

## 📊 当前服务全景（全部 active + 开机自启）

| 服务 | 端口 | 用途 |
|---|---|---|
| `frpc-dsh-web` | 6680 | DSH 公网访问隧道 |
| `zlh-website` | 9500 | 官网 + 注册后端 |
| `stepclaw-gateway` | 30999 | DSH 子 agent（`subagent_stepclaw`）网关 |
| `openclaw-gateway` | 18789 | 独立 openclaw 网关 |

**失败服务：0 个。**

## ⚠️ 遗留

- `00-Home/导航.md` 的 `[[03-Tasks/任务模板]]` 等历史死链：**按用户要求保留 6 个月不删**
- 节点1 已丢弃、凭据未清理，待轮换清单见工作区 `CREDENTIALS-TO-ROTATE.md`（最紧急：frp token，本机 4 个配置在用）
