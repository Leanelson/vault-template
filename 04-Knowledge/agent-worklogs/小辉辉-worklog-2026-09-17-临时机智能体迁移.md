---
type: agent-worklog
agent: "小辉辉"
session: "dsh-suibi-2026-09-17-tmpnode-migration"
created: 2026-09-17 09:10
tags: [🤖-agent, 📝-worklog]
---

# 🤖 Agent 工作记录

**Agent：** 小辉辉（DSH）
**Session：** dsh-suibi-2026-09-17-tmpnode-migration
**时间：** 2026-09-17 09:10

## 📋 本次工作摘要

把**临时机智能体**（`/root/.openclaw`，情报中枢节点）的全量数据——**会话历史 + 14 个项目 + agent 本体源码**——
经节点2 中转，全部回迁本机并**三方哈希对齐**；随后归并去重（9.1G → 840M）、提取精华、
撤销全部一次性密钥与通道。**未接管其定时任务**（避免与本机重复）。

## 🔧 执行的操作

1. 设计**只能用聊天消息**驱动的传输协议：对端自生成密钥对 → 我登记公钥 → 30MB 分片 push 到中转机，
   本机 pull 并合并校验；每类单据式命令≤10 片（对端有 ~55 分钟命令超时）。
2. 六件套分四批完成（b3/b4/b5/b6）+ 早前 batch1/2；**对端原始 sha256 = 中转机合并 sha256 = 本机落袋 sha256**，逐一对平。
3. 归并归档 `tmpnode-archive/{live,history,source,config}`：现役版为准、四月版留一份、剔除构建产物（Rust target / node_modules）。
4. 精华提取：`essence/hub-jobs.md`（3 个 cron 任务）、`essence/session-index.md`（**143 条会话**）。
5. 安全收口：撤销 `migration-20260915`、`tmpnode-push` 两条公钥，**用自己的一次性私钥实测 Permission denied 证明失效**。

## 📌 关键产出

| 项 | 值 |
|---|---|
| 精选归档 | `suibi/tmpnode-archive/`（**840M**） |
| 原始六件套 | `suibi/tmpnode-handoff/inbox/`（2.8G） |
| 会话索引 | `suibi/tmpnode-handoff/essence/session-index.md`（143 条：86 存活 / 53 软删除救回 / 4 重置） |
| 情报中枢任务 | `suibi/tmpnode-handoff/essence/hub-jobs.md` |
| 消化总报告 | `suibi/tmpnode-handoff/DIGEST-REPORT.md` |
| 会话时间线 | 2026-03-24 → 09-16（⚠️ 4/07–4/14 缺口不可恢复） |

## ⚠️ 待跟进

1. **情报中枢的 3 个定时任务已随机器下线**（招标日报、半小时情报增量、自动巡检）——本机是否接管，
   用户明确"暂不，后期再说；接管可以但不要重复"。
2. 中转机 `D:\tmpnode_inbox\migrate-01` 的 2.8G 副本：精华提取与经验沉淀完成后可清（用户已授权）。
3. 记忆库（`memory/*.sqlite`）**是空的**（chunks=0）——它从未启用向量记忆，历史只存在于会话 jsonl 里。

## 💭 反思/学习

- **软删除不等于删除**：`*.jsonl.deleted.*` 改名即抢救，53 个会话因此保住。
- **低带宽下分片粒度 = 恢复粒度**；命令超时是最大杀手，宁可多条短命令。
- **校验要三方对齐**：对端自报的哈希不可全信（它也曾误判自己的分片损坏），落袋哈希才是终点。
